import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  template: '<app-mission-control></app-mission-control>'
})
export class AppComponent {
  title = 'component-interaction';
}